frag_lib    ./example/input_reads_frag.?.fastq ; .fq, .fastq.gz, .fq.gz, * and ? in names are supported. Skip this line if no jumping library is available
insert_size 220
stddev      50

jump_lib    ./example/input_reads_jump.fastq ; .fq, .fastq.gz, .fq.gz, * and ? in names are supported. Skip this line if no jumping library is available
insert_size 3000
stddev      0

contigs_for_sim ./example/sim_contigs.fasta ; skip this line if no simulation needed

ploidy          1  ; 1 for haploid genomes (default) and 2 for diploid ones
output_contigs  ./example/output_contigs.fasta

