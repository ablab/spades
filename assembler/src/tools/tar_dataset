#!/bin/bash

DIR="$( cd "$( dirname "$0" )" && pwd )"
BASE="$( cd "$( dirname "$0" )" && cd ../.. && pwd )"
pushd $BASE/data/input > /dev/null
pwd
python $BASE/src/tools/dataset.py tar $BASE/src/debruijn/datasets.info $1
